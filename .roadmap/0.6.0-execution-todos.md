# kcspoc v0.6.0 Execution-Level TODOs

This file tracks granular, execution-level tasks derived from the high-level [0.6.0-dev.md](file:///home/arturscheiner/CodePlace/Kaspersky/kcspoc/.roadmap/0.6.0-dev.md) roadmap.

---

## 🏗 MVC Architecture Refactor
- [x] Initialize directory structure: `/lib/model`, `/lib/view`, `/lib/control`
- [x] Refactor `kcspoc.sh` to implement automatic component discovery and sourcing
- [x] Migrate all UI/output logic (colors, icons, headers) to `/lib/view`
- [x] Migrate kubectl and filesystem wrappers to `/lib/model`
- [x] Establish standard "Controller" orchestration for existing commands
- [x] Implement component-based version hashing for robust versioning

## 🎨 Universal UI & Visual Identity (Revamp)
- [ ] Define standardized, high-contrast color palette in `base_view.sh` (Green/Red/Yellow/Orange)
- [ ] Implement `view_ui_table_header` and `view_ui_table_row` standardized helpers
- [ ] Audit and refactor `logs --list` and `pull --list-local` tables for structural consistency
- [ ] Audit and refactor all command headers to use `ORANGE` visual identity

## 🛡 CNI Network Policy Enforcement Validation
- [ ] Create `lib/model/network_model.sh` for diagnostic pod and policy management
- [ ] Implement connectivity validation logic in `lib/service/check_service.sh`
- [ ] Integrate functional NetworkPolicy verification into `kcspoc check --deep`

## 🤖 AI-Powered Capabilities (Ollama)
- [ ] Create `lib/service/ai_service.sh` for standardized Ollama interaction
- [ ] Implement `kcspoc logs --analyze <hash>` for AI-driven log interpretation
- [ ] Implement `kcspoc logs --report <hash>` for post-execution reporting
- [ ] Implement `kcspoc check --ai-eval` for diagnostic assistance
- [ ] Update configuration wizard to support specialized AI model selection

## 🚀 Workflows & Guided Execution
- [ ] Implement `--unattended` mode for `deploy` and `destroy` commands
- [ ] Orchestrate guided deployment walkthrough for new users (interactive hints)
- [ ] Improve post-execution summaries with actionable recommendations
- [x] Implement granular component installation in `kcspoc prepare` via `--install [comma-separated-list]`
    - [x] Extend `prepare_controller.sh` parser for `--install`
    - [x] Add component validation logic
    - [x] Implement selective execution in `prepare_service.sh`
- [ ] Implement advanced lifecycle in `kcspoc prepare`
    - [ ] Support `--install all` shortcut
    - [ ] Implement `--uninstall [all|components]` flag and logic
    - [ ] Update help UI and localization for new options

## � Enhanced Configuration & Isolation
- [ ] Implement support for `KCSPOC_NAMESPACE` to isolate tool management
- [ ] Ensure sample vulnerable pods and test workloads are strictly isolated
